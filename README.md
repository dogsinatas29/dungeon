이동 : 방향키 <br>
게임 실행 : python3 Start.py<br>

한글 메시지 출력은 고쳤습니다. <br>
이동 중 맵이 변경되는 버그가 있습니다<br>
바이브 코딩으로 만든 심시풀이 땅콩 던전 크롤 게임입니다. <br>
뭐랄까 디아블로를 로그라이크로 만든 것 같은 느낌을 주고 싶은 게임이 목표입니다<br>
현재 렌더링 순서로 인한 맵 밀림 버그가 있습니다. 레이어 방식 렌더링으로 수정중입니다.<br>

`dungeon_map.py` 변경: 맵의 배경(벽, 바닥, 아이템, 시체 등) 정보만 반환하도록 get_tile_for_display
      함수를 수정합니다. 이 함수는 더 이상 플레이어나 살아있는 몬스터의 위치를 알 필요가 없습니다. 이렇게 되면 순수한 배경 정보만 반환합니다. 
`ui.py` 변경: draw_game_screen 함수가 다음과 같은 순서로 맵을 그리도록 완전히 재작성합니다.
       * 1단계 (배경 그리기): dungeon_map.py의 수정된 함수를 호출하여, 순수한 배경 맵을 그립니다.
       * 2단계 (개체 그리기): 배경 위에 플레이어와 살아있는 몬스터들을 "덧그립니다(Overlay)".
       * 이 모든 과정에서 문자의 실제 너비를 정확히 계산하여, 각 줄이 절대 뷰포트 너비를 넘지 않도록
         합니다.

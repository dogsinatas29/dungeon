2025년 11월 22일 - 이벤트 시스템 통합:
- `events` 디렉토리와 `events/event_manager.py`, `events/game_events.py` 파일 생성 및 구현.
- `dungeon/system.py`의 `CollisionSystem`을 수정하여 `PlayerMovedEvent`를 발행하도록 함.
- `dungeon/system.py`에 `LoggingSystem`을 추가하여 `PlayerMovedEvent`를 구독하고 몬스터 근접 메시지를 처리하도록 함.
- `dungeon/system.py`의 `RenderingSystem`을 수정하여 `PlayerMovedEvent`를 구독하고 카메라 계산 및 기본 UI 상태로 렌더링을 처리하도록 함.
- `dungeon/engine.py`에서 시스템 초기화를 업데이트하고, 전역 UI 상태 변수를 제거했으며, 카메라 계산 및 직접적인 `rendering_system.update()` 호출을 제거하고, `collision_result` 메시지 처리를 조정함.
- `events/event_manager.py`의 싱글톤 구현을 `_initialized` 플래그를 사용하여 더욱 견고하게 개선함.
- `events/game_events.py`의 `PlayerMovedEvent`에 `entity_id` 속성을 추가.
- `dungeon/system.py`의 `CollisionSystem`에서 `PlayerMovedEvent` 발행 시 `entity_id`를 포함하도록 수정.
- `dungeon/system.py`에 `System` 기본 클래스 정의를 추가.
- `dungeon/system.py`의 `LoggingSystem`을 `System`을 상속하도록 변경하고, 생성자 및 `handle_player_moved_event` 로직을 업데이트하여 단순화된 플레이어 이동 메시지만 로깅하도록 수정.
- [통합] `dungeon/engine.py`에 `LoggingSystem` 초기화 및 통합 완료.
- `dungeon/component.py`에 `ColliderComponent`를 추가하고 중복된 `ItemUseRequestComponent` 정의를 제거.
- `dungeon/system.py`의 `CollisionSystem.update()`를 수정하여 `ColliderComponent`를 활용한 일반화된 엔티티 충돌 감지를 수행하도록 변경.